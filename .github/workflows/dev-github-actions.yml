name: dev-github-actions
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: make compile
  contract-testing:
    if: ${{ success() }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "contract-testing"
  sonarcloud:
    runs-on: ubuntu-latest
    steps:
      - run: echo "sonarcloud"
  terraform:
    runs-on: ubuntu-latest
    steps:
      - run: echo "terraform"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "deploy"
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "smoke-test"